apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: signoz
  namespace: platform
spec:
  ingressClassName: kong
  rules:
  - host: lokidashboard.test.com
    http:
      paths:
      - backend:
          service:
            name: szlog-signoz-frontend
            port:
              number: 3301
        path: /
        pathType: Prefix
